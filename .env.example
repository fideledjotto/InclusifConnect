APP_NAME="Inclusif Connect"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

# Domaine(s) du frontend autorisé(s) pour les cookies Sanctum (SPA)
SANCTUM_STATEFUL_DOMAINS=localhost:3000,127.0.0.1:3000
SESSION_DOMAIN=localhost
FRONTEND_URL=http://localhost:3000

LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=inclusif_connect
DB_USERNAME=root
DB_PASSWORD=

FILESYSTEM_DISK=public

MAIL_MAILER=log
MAIL_FROM_ADDRESS="contact@inclusif-connect.org"
MAIL_FROM_NAME="${APP_NAME}"
